<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="45dp"
    android:background="@drawable/back12"
    tools:context=".MasterFragments.MasterTeachers">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/add_teacher_recycler"
        android:backgroundTint="@color/transparent"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentTop="true"
        android:background="@color/white"
        android:padding="4dp"
        android:scrollbars="vertical"
        android:layout_above="@id/add_teacher"/>

    <Button
        android:id="@+id/add_teacher"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:padding="10dp"
        android:layout_margin="40dp"
        android:text="@string/add_teacher"/>
</RelativeLayout>
